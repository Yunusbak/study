1.
from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.

def get_info(request):
    return HttpResponse("Hi Boy!")
def get_name(request):
    return HttpResponse("My name is Yunus")



from django.urls import path
from .views import get_info, get_name

urlpatterns = [
    path("app_url", get_info, ),
    path("app2_url", get_name),
]



from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path("main_url/", include("home.urls")),
    path("main_url/", include("home.urls")),
]
2.
from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.

def view1(request):
    return HttpResponse("hello!")
def view2(request):
    return HttpResponse("My name is Yunus")



from django.urls import path
from .views import view1, view2

urlpatterns = [
    path("app_url", view1, ),
    path("app2_url", view2),
]



from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path("main_url/", include("home.urls")),
    path("main_url/", include("home.urls")),
]
3.
from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.

def view_3(request):
    return HttpResponse("salom!")
def view3_1(request):
    return HttpResponse("bob")



from django.urls import path
from .views import view_3, view3_1

urlpatterns = [
    path("app_url", view_3, ),
    path("app2_url", view3_1),
]



from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path("main_url/", include("home.urls")),
    path("main_url/", include("home.urls")),
]
4.
from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.

def get_info(request):
    return HttpResponse("Hi Boy!")
def get_name(request):
    return HttpResponse("My name is Yunus")



from django.urls import path
from .views import get_info, get_name

urlpatterns = [
    path("app_url", get_info, ),
    path("app2_url", get_name),
]



from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path("main_url/", include("home.urls")),
    path("main_url/", include("home.urls")),
]
5.
from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.

def get_info(request):
    return HttpResponse("Hi Boy!")
def get_name(request):
    return HttpResponse("My name is Yunus")



from django.urls import path
from .views import get_info, get_name

urlpatterns = [
    path("app_url", get_info, ),
    path("app2_url", get_name),
]



from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path("main_url/", include("home.urls")),
    path("main_url/", include("home.urls")),
]
6.
from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.

def get_info(request):
    return HttpResponse("Hi Boy!")
def get_name(request):
    return HttpResponse("My name is Yunus")



from django.urls import path
from .views import get_info, get_name

urlpatterns = [
    path("app_url", get_info, ),
    path("app2_url", get_name),
]



from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path("main_url/", include("home.urls")),
    path("main_url/", include("home.urls")),
]
7.
from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.

def get_info(request):
    return HttpResponse("Hi Boy!")
def get_name(request):
    return HttpResponse("My name is Yunus")



from django.urls import path
from .views import get_info, get_name

urlpatterns = [
    path("app_url", get_info, ),
    path("app2_url", get_name),
]



from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path("main_url/", include("home.urls")),
    path("main_url/", include("home.urls")),
]
1.
from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.

def get_info(request):
    return HttpResponse("Hi Boy!")
def get_name(request):
    return HttpResponse("My name is Yunus")



from django.urls import path
from .views import get_info, get_name

urlpatterns = [
    path("app_url", get_info, ),
    path("app2_url", get_name),
]



from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path("main_url/", include("home.urls")),
    path("main_url/", include("home.urls")),
]
8.
from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.

def get_info(request):
    return HttpResponse("Hi Boy!")
def get_name(request):
    return HttpResponse("My name is Yunus")



from django.urls import path
from .views import get_info, get_name

urlpatterns = [
    path("app_url", get_info, ),
    path("app2_url", get_name),
]



from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path("main_url/", include("home.urls")),
    path("main_url/", include("home.urls")),
]
9.
from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.

def get_info(request):
    return HttpResponse("Hi Boy!")
def get_name(request):
    return HttpResponse("My name is Yunus")



from django.urls import path
from .views import get_info, get_name

urlpatterns = [
    path("app_url", get_info, ),
    path("app2_url", get_name),
]



from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path("main_url/", include("home.urls")),
    path("main_url/", include("home.urls")),
]
10.
from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.

def misol(request):
    return HttpResponse("10 misol!")
def masala_x(request):
    return HttpResponse("10 misolni2 view")



from django.urls import path
from .views import misol, masala_x

urlpatterns = [
    path("app_url", misol, ),
    path("app2_url", masala_x),
]



from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path("main_url/", include("home.urls")),
    path("main_url/", include("home.urls")),
]
